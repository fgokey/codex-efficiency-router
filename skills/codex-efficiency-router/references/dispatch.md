# Native dispatch and guard

Wait for writers; leaves never spawn. Sol/Terra delivery needs explicit parent handoff of user authorization, repository/ref/destination and checks. Handoff grants no new authority.

## Native change summary and review

Before write routing, compare target repositories with task workspace roots. A required parent badge needs native file-change attribution in that workspace. Verify host support for parent writes or child aggregation; model name alone proves neither. If mandatory with support unknown, stop before writes and report the gap. Native review/open-review does not populate that badge.

Return every repository root, exact changed paths/status and owned vs pre-existing dirt. Parent verifies status/diff per repository and opens unstaged review. Reuse valid checks; inspect critical integration, fetch full logs only on discrepancy. Never touch/reapply files only for attribution.

The optional strict PreToolUse Guard denies every Astra/unknown shell, patch and unknown tool. Active disables the root exception; UNKNOWN may still deny at runtime. Reads/coordination work.

Astra NEVER inputs to existing shells: `write_stdin` is UNPROTECTED. Original executor only. Hosted paths UNPROTECTED; specialized paths UNKNOWN.

With an active Guard, `cer-read` supports read/list/search/diff/status and flat batches. It is a rewrite protocol, not a shell binary; never replay it inactive. Shell/build/test belongs to executors.

Batch: 1-16 requests; 16 KiB input, 16 MiB reads, 128 KiB output. Preflight paths; invalid batch has no partial output. Search is one-file literal; no commands.
